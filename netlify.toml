# https://www.netlify.com/docs/netlify-toml-reference/
[build]
  base = "./"
  publish = "./_site"
  command = "npm run build && npm run build:site"
  functions = "api"
[dev]
  command = "bundle exec jekyll build --drafts --unpublished --future --profile --trace --strict_front_matter"
